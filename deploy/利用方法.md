# 初回起動
```bash
$ cd deploy
$ docker compose up -d --build
```

## サンプルアプリへのアクセス URL
- http://localhost:8080/bom

# 停止
- docker compose stop

# 2 回目以降の起動
- docker compose start

# サンプル停止・コンテナ削除
- docker compose down
